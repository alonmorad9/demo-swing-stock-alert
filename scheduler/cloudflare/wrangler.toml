name = "swing-stock-alert-scheduler"
main = "worker.js"
compatibility_date = "2026-05-04"

[triggers]
# Paused on 2026-05-23: this repo is now a research archive.
# Keep manual GitHub workflow runs available, but do not schedule automatic reports.
crons = []
